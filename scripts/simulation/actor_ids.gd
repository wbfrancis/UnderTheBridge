class_name ActorIds
extends RefCounted

## Actor identity is a fixed positive integer, unique across Patrons and Cultists.
## Names and actor kinds are data; never infer either from the number.
const NO_ACTOR: int = 0
const CULTIST_IDS: Array[int] = [1, 2, 3]
