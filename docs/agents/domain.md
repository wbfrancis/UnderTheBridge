# Domain docs

Before planning or implementing work, read the root `CONTEXT.md` and any relevant ADRs under `docs/adr/`.

Use the glossary’s exact vocabulary in issues, specifications, code, and tests. When a required concept is absent, revisit it with `domain-modeling`.

Surface conflicts with an existing ADR explicitly instead of silently overriding it.

This repository uses a single domain context.
